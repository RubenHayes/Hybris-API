$kind: http-request
name: Hybris Variable Setup
url: "{{grocLogin}}"
method: POST
headers:
  Content-Type: application/x-www-form-urlencoded
  Authorization: "{{oneLoginAuth}}"
  Accept: "*/*"
body:
  type: text
  content: "{{one1TFA}}"
auth:
  type: noauth
scripts:
  - type: beforeRequest
    code: "//Test Comment\r\n// Ruben Test"
    language: text/javascript
order: 1000
