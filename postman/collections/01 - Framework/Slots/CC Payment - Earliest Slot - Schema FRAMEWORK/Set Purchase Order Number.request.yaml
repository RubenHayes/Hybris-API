$kind: http-request
name: Set Purchase Order Number
url: "{{pnpOne}}/users/current/carts/{{cart}}/purchaseorder"
method: PUT
headers:
  Origin: "{{oneOriginHeader}}"
body:
  type: urlencoded
  content:
    purchaseOrderNumber: apiAuto1
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: afterResponse
    code: "// ===========================================\r

      // Request: PUT /users/current/carts/{{cart}}/purchaseorder\r

      // Purpose: Add Purchase Order Number to Cart\r

      // Expected status: 200 OK\r

      // ===========================================\r

      \r

      // 🧩 ~~ Generic Uptime Tests ~~ 🧩\r

      const assertStatus = eval(pm.globals.get(\"assertStatus\"));\r

      assertStatus(200, \"OK\");\r

      \r

      // Conditional block\r

      if (pm.response.code === 200) {\r

      \r

      \    // No jsonData needed — Hybris returns no body for Set Delivery Address\r

      \r

      \    // === Console Message ===\r

      \    console.log(`===== ${pm.info.requestName} - PASS =====`);\r

      \r

      } else {\r

      \r

      \    // === Console Message ===\r

      \    console.log(`===== ${pm.info.requestName} - FAIL =====`);\r

      \    console.log(pm.response.text());\r

      }"
    language: text/javascript
order: 12000
