$kind: http-request
url: "{{pnpOne}}/users/current/carts/{{cart}}/slotreservation"
method: PUT
headers:
  origin: "{{oneOriginHeader}}"
body:
  type: urlencoded
  content:
    slotCode: "{{deliverySlot}}"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: afterResponse
    code: "// ===========================================\r

      // Request: PUT {{groceries}}/users/current/carts/{{cart}}/slotreservation\r

      // Purpose: Set slot reservation for current cart\r

      // Auth: Bearer token {{accessToken}}\r

      // Expected status: 200 OK\r

      // ===========================================\r

      \r

      // 🧩 ~~ Test Configuration ~~ 🧩\r

      var environmentFlag = pm.environment.name; // Get the current environment name\r

      \r

      // 🧩 ~~ Generic Uptime Tests ~~ 🧩\r

      pm.test(\"[Carts Controller] Add Slot Reservation - Status code is 200\", () => {\r

      \    pm.response.to.have.status(200);\r

      });\r

      \r

      pm.test(\"[Carts Controller] Add Slot Reservation - Response status is OK\", () => {\r

      \    pm.expect(pm.response.status).to.eql(\"OK\");\r

      });\r

      \r

      // Conditional block\r

      if (pm.response.code === 200) {\r

      \r

      \    // No jsonData needed — Hybris returns no body for Add Slot Reservation\r

      \r

      \    // === Console Message ===\r

      \    console.log(`===== ${pm.info.requestName} - PASS =====`);\r

      \r

      } else {\r

      \r

      \    // === Console Message ===\r

      \    console.log(`===== ${pm.info.requestName} - FAIL =====`);\r

      \    console.log(pm.response.text());\r

      }"
    language: text/javascript
order: 18000
